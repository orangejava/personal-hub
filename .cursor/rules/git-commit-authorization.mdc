---
description: 仅在用户明确授权时才可执行 git commit / push
alwaysApply: true
---

# Git 提交授权规则（强制）

## 何时可以 commit

**只有**用户在本轮对话中明确授权时，才允许执行 `git commit`。

视为授权的说法示例：

- 「提交」「帮我 commit」「执行第 N 批 commit」
- 「按计划提交第 1 批」
- 「可以 commit 了」

## 何时禁止 commit

以下情况**一律不得**执行 `git commit`：

- 只完成了功能开发或文档编写，用户未提提交
- 用户只说「写好规范 / 写好计划 / 先 stage」
- 用户说「先不要提交」「暂不 commit」
- 仅询问 commit message 该怎么写

## push 规则

- `git push` 同样需要用户明确授权（如「推送到 GitHub」「push」）
- 未授权时只 commit 到本地，或只给出版本状态说明

## 提交前必须遵守

1. 遵循仓库文档 `docs/engineering/git-commit-convention.md`
2. 使用 Conventional Commits；message 用 HEREDOC 传入
3. 不提交密钥、`content-local/`、`node_modules/`、生成物
4. 不使用 `--no-verify` 绕过 hook（除非用户明确要求）
5. 不修改 git config；不做 force push main（除非用户明确要求）

## 分批提交

本仓库采用分批入库。即使用户说「全部提交」，也按批次依次 commit，并在对话中列出本批内容与剩余批次。
