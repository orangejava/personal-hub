import React from 'react';
import { PageContainer } from '@/components/shared';

/** 后台首页（阶段 0-3 仅占位） */
const Home: React.FC = () => (
  <PageContainer title="后台首页">
    <p>后台完整 CRUD 在阶段 4 实现，本阶段仅保留入口与占位。</p>
  </PageContainer>
);

export default Home;
