import type { PublicThemeSettings } from '@/types/app';

/** 公开前台默认主题（与工作区 ProLayout settings 独立） */
export const publicDefaultSettings: PublicThemeSettings = {
  navTheme: 'light',
  colorPrimary: '#1677ff',
};
