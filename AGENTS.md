# 项目协作规范

## Agent 启动必读（强制）

每次开始任务时，**必须先阅读**：

1. **本文件** `AGENTS.md`
2. **`.agents/rules/`** 下全部规则（当前含文档放置、开发工作流）
3. 与当前任务匹配的 **`.agents/skills/*/SKILL.md`**
   - 涉及 `git commit` / `git push` / 写 commit message / 「提交」「推送」→ **必须**加载 [`.agents/skills/git-commit/SKILL.md`](.agents/skills/git-commit/SKILL.md)

说明见 [`.agents/README.md`](.agents/README.md)。

未读上述内容时：不得擅自 commit、不得违反文档目录约定、不得做破坏性 git 操作。

目录约定：跨工具使用 **`.agents/`**（复数、点前缀），不是 `agent/`。Cursor 专用薄规则在 `.cursor/rules/`，以指向 `.agents` 为准，避免双源。

---

## 工作前：先找上下文

每次开始任务前，**先通过以下文档找到当前项目的背景**，再分析和动手：

| 文档 | 作用 |
|---|---|
| `docs/overview.md` | **先读这里**：产品定位、整体结构、子文档目录、待确认清单 |
| `docs/README.md` | **文档目录职责**：放哪里 / 不放哪里（防文档散乱） |
| `.agents/skills/git-commit/SKILL.md` | Git 提交权威规范（授权 + 格式 + 步骤） |
| `docs/foundation/tech-stack.md` | 技术栈选型与工程架构 |
| `docs/product/frontend-public.md` | 公开前台页面与功能 |
| `docs/product/workspace.md` | 登录后工作区 |
| `docs/product/admin.md` | 后台管理台 |
| `docs/product/auth-rbac.md` | 用户、登录、权限体系 |
| `docs/product/content-system.md` | 知识内容模型与格式 |
| `docs/product/ai-tools.md` | AI 工具平台与接入策略 |
| `docs/product/flutter.md` | Flutter App 范围与接入 |
| `docs/deploy/README.md` | 部署文档入口（个人远程 / 生产） |

**规则**：
- 涉及哪个模块，就先读对应文档，而不是凭印象或猜测
- **文档必须按 `docs/README.md` 与 `.agents/rules/docs-placement.md` 放置**；部署类禁止写入 `docs/prd/`
- 文档中有「待细化」或「待讨论」标注的内容，需先与用户确认，不要擅自做决定
- 讨论有新结论时，同步更新对应文档；新增 docs 后更新 `docs/overview.md` 或子目录 README

## 先规划，后开发

细则见 `.agents/rules/dev-workflow.md`。摘要：

- 在开始修改代码前，先给出清晰的实现计划。
- 计划应至少说明：**现象/需求 → 根因或约束 → 解决方案 → 影响范围 → 计划修改的文件**。
- 在思路未说明清楚前，不要直接开始改代码。
- 需求不明确时，先澄清再实施；思路明确后，按计划逐步实现。

## 分析优先

- 执行任务时，优先说明问题分析过程，而不是直接给出改动结果。
- 如果存在多种实现方案，需要先说明取舍理由，再选择更稳妥、影响面更小的方案。
- 回答和说明默认使用中文。

## 中文注释与文档

- 对**关键方法、复杂逻辑、核心状态变量、关键配置项**补充中文注释，重点说明“为什么这样做”以及业务含义，避免逐行翻译代码。
- 对关键方法优先补充 **JSDoc / 文档注释**，用中文说明：
  - 方法用途
  - 参数含义
  - 返回值含义
  - 必要时补充边界条件、前置条件或副作用
- 对关键变量在首次定义或语义不直观时补充中文注释，说明其业务含义、单位、取值约束或生命周期。
- 简单直白、语义自解释的代码无需为了“有注释而注释”。

## 功能完成后的学习与开发沉淀

本项目是“边开发边学习”的长期项目。AI 完成一个功能后，除了交付代码，还要帮助用户理解这次开发为什么这样做、以后遇到类似需求如何自己实现。

**规则**：
- 开发前的需求细化、页面逻辑拆解、接口草案，统一放在 `docs/prd/`。
- **部署、服务器目录、上线命令、备份**，统一放在 `docs/deploy/`。
- **Git 提交规范与流程**，统一以 `.agents/skills/git-commit/` 为准（不要在 `docs/` 再维护完整副本）。
- 功能开发完成后的实现说明、调用链、联调约定、落地记录，统一放在 `docs/implementation/` 下，按模块分子目录存放，例如：`docs/implementation/content/markdown-reader.md`。
- 功能对应的学习文档统一放在 `study/features/` 下，命名优先使用“模块-主题”形式，例如：`study/features/content-markdown-reader.md`。
- 通用型、长期复用的技术手册继续放在 `study/` 根目录；不要把单次功能复盘和通用手册混放。
- 新增 `docs/implementation/` 或 `study/features/` 文档后，需要同步更新对应目录下的 `README.md` 或总入口索引，保证后续可查找。
- 每个功能完成后，交付说明中补充“学习与开发思路”小结，说明：
  - 本次需求拆解方式
  - 关键技术点或框架机制
  - 核心调用链 / 数据流 / 状态流
  - 重要文件各自承担的职责
  - 本次踩坑点、取舍理由或可扩展方向
- 如果本次功能引入了新的模块、架构约定、接口约定、权限规则、数据模型或部署方式，需要同步更新 `docs/` 下对应项目文档。
- 如果本次功能包含值得反复学习的技术点、排障过程或完整实现套路，需要在 `study/` 下新增或更新学习文档，并在 `study/README.md` 中补充入口。
- 学习文档应优先写成可复用的开发笔记，而不是单次流水账。建议包含：
  - 场景背景：这类问题通常什么时候出现
  - 核心概念：需要先理解哪些知识
  - 实现步骤：从入口、状态、接口、数据到验证的顺序
  - 关键代码：只摘录最能说明问题的片段
  - 常见错误：本项目中容易踩坑的位置
  - 手动验证：用户可以如何自己确认功能正确
- 小型修复或纯样式微调不强制新增学习文档，但仍需在交付说明中解释改动思路和验证方式。
- 如果不确定是否需要新增学习文档，先说明判断依据；功能较复杂、用户明确表示想学习时，默认沉淀到 `study/`。

## 新增页面与样式约束

- 新增页面、组件或交互界面时，如果未提供明确设计稿、截图或视觉规范，优先参考**当前项目内、就近目录下的同类页面**。
- 尽量复用现有布局、间距、配色、字号、组件用法和交互节奏，保持视觉与交互一致。
- 不要无依据地新起一套样式体系或交互模式。

## 安全增量开发

细则见 `.agents/rules/dev-workflow.md`。摘要：

- 只修改与当前需求直接相关的文件和逻辑，不顺手重构无关代码。
- 修改**共享组件、工具函数、全局 store、请求封装、路由链路**前，先确认调用方和当前既有行为，避免破坏现有交互和业务流程。
- 不随意替换底层 API、第三方组件约定或现有封装方式；优先沿用项目内已有实现模式。
- 避免通过临时、脆弱或绕过状态管理的方式实现功能，例如直接操作 DOM 内容导致状态失步。

## 改动前说明要求

- 动手修改前，先用简短中文说明：
  - 当前现象或目标
  - 原因或限制
  - 解决思路
  - 计划改动的文件
- 如果方案存在风险、兼容性差异或影响共享逻辑，需要提前说明。

## 改动后检查要求

- 交付前确认原有核心交互仍可正常工作，例如按钮、快捷键、弹窗、点击外部关闭、表单提交、筛选、路由跳转等。
- 避免因为局部改动影响其他模块、页面或公共能力。
- 涉及布局调整时，注意不要挤压固定操作区、工具栏、侧栏或已有响应式结构。
- 若仓库已有对应的构建、测试、校验命令，应按现有方式执行相关验证。

## 前端页面验收

- 完成前端页面、组件、样式或交互相关改动后，再宣布完成前，应先做可行的验收。
- 如果环境支持内置浏览器能力，应检查：
  - 页面是否可正常打开
  - 目标区域 DOM 和交互元素是否符合预期
  - 关键操作流程是否可执行
  - 控制台中是否出现与本次改动相关的新报错
  - 涉及样式时，页面视觉结果是否符合预期
- 如果当前环境无法直接进行浏览器验收，需要明确说明原因，并提供可复现的手动验证步骤。

## 交付说明

- 回复时写明本次改动可能影响的功能点。
- 给出 2～3 步用户可执行的手动验证路径。
- 如果无法在当前环境完成某项验证，需要明确说明依据，例如代码路径、调用链或行为推断来源。
